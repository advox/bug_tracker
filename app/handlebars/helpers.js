module.exports = {
    dateYmdHis: function (dateString) {
        const dateTime = require('node-datetime');

        // return dateTime.create(dateString).format('Y-m-d H:I:s')
        return ';;;;';
    }
};